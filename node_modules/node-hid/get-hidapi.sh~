#!/bin/sh

if [ -f .git ]
then
    git submodule update -r -i
else
    if [ -d hidapi ]
    then
        cd hidapi
        git pull
    else
        git clone git://github.com/signal11/hidapi
    fi
fi